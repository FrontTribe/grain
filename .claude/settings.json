{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "command -v grain >/dev/null 2>&1 && grain hook claude || true"
          }
        ]
      }
    ]
  }
}
