# Go
/bin/
/dist/
*.exe
*.test
*.out
vendor/

# Grain output (generated) — only the one grain writes at the repo root
/grain.json
# PROVENANCE.md is intentionally committable — do not ignore it

# Node (npm shim)
node_modules/
npm/vendor/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env / secrets
.env
.env.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
